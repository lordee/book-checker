{
  "name": "book-checker-monorepo",
  "version": "1.0.10",
  "scripts": {
    "dev:backend": "cd book-checker/backend && npm run dev",
    "dev:frontend": "cd book-checker/frontend && npm run dev",
    "dev": "npm run dev:backend & npm run dev:frontend",
    "install:all": "cd book-checker/backend && npm install && cd ../frontend && npm install",
    "build:all": "cd book-checker/backend && npm run build && cd ../frontend && npm run build"
  }
}
